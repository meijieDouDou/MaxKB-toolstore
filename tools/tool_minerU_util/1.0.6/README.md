## 1.0.6 版本说明

**注意**：当前适用的 MaxKB 版本为 v2.10.5-lts 及以上版本，MinerU（本地 Gradio 服务）版本为 3.4.4。

## 注意事项

- 仅支持**离线部署**的 MinerU，且解析服务需以 Gradio 形式暴露（默认 `http://<MinerU 服务 ip>:7860/`）
- 1.0.6 起容器内**无需**再安装 `gradio_client`；若此前按 1.0.5 的文档装过，保留即可、不必卸载
